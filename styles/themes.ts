import type { Themes } from 'types/styles/themes';

const defaultTheme = {
  colors: {
    primary: '#000',
    window: '#808080'
  }
};

const themes: Themes = { defaultTheme };

export default themes;
